---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: databases.database.vultr.upbound.io
spec:
  group: database.vultr.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - vultr
    kind: Database
    listKind: DatabaseList
    plural: databases
    singular: database
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Database is the Schema for the Databases API. Provides a Vultr
          database resource. This can be used to create, read, modify, and delete
          managed databases on your Vultr account.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseSpec defines the desired state of Database
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessCert:
                    description: The certificate to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  accessKey:
                    description: The private key to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  backupHour:
                    description: The preferred hour of the day (UTC) for daily backups
                      to take place (unavailable for Kafka engine types).
                    type: string
                  backupMinute:
                    description: The preferred minute of the backup hour for daily
                      backups to take place (unavailable for Kafka engine types).
                    type: string
                  clusterTimeZone:
                    description: The configured time zone for the Managed Database
                      in TZ database format (e.g. UTC, America/New_York, Europe/London).
                    type: string
                  databaseEngine:
                    description: The database engine of the new managed database.
                    type: string
                  databaseEngineVersion:
                    description: The database engine version of the new managed database.
                    type: string
                  enableKafkaConnect:
                    description: The configuration value for Kafka Connect support
                      (Kafka engine types only).
                    type: boolean
                  enableKafkaRest:
                    description: The configuration value for Kafka REST support (Kafka
                      engine types only).
                    type: boolean
                  enableSchemaRegistry:
                    description: The configuration value for Schema Registry support
                      (Kafka engine types only).
                    type: boolean
                  evictionPolicy:
                    description: The configuration value for the data eviction policy
                      on the managed database (Valkey engine types only - noeviction,
                      allkeys-lru, volatile-lru, allkeys-random, volatile-random,
                      volatile-ttl, volatile-lfu, allkeys-lfu).
                    type: string
                  ferretdbCredentials:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  kafkaRestUri:
                    description: The URI to access the RESTful interface of your Kafka
                      cluster if Kafka REST is enabled (Kafka engine types only).
                    type: string
                  label:
                    description: A label for the managed database.
                    type: string
                  maintenanceDow:
                    description: The preferred maintenance day of week for the managed
                      database.
                    type: string
                  maintenanceTime:
                    description: The preferred maintenance time for the managed database.
                    type: string
                  mysqlLongQueryTime:
                    description: The configuration value for the long query time (in
                      seconds) on the managed database (MySQL engine types only).
                    type: number
                  mysqlRequirePrimaryKey:
                    description: The configuration value for whether primary keys
                      are required on the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSlowQueryLog:
                    description: The configuration value for slow query logging on
                      the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSqlModes:
                    description: A list of SQL modes to configure for the managed
                      database (MySQL engine types only - ALLOW_INVALID_DATES, ANSI,
                      ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                      IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE, NO_ENGINE_SUBSTITUTION,
                      NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE, NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY,
                      PIPES_AS_CONCAT, REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                      TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  password:
                    description: The password for the managed database's primary admin
                      user.
                    type: string
                  plan:
                    description: The ID of the plan that you want the managed database
                      to subscribe to. See List Managed Database Plans
                    type: string
                  planBrokers:
                    description: The number of brokers available on the managed database
                      (Kafka engine types only).
                    type: number
                  planDisk:
                    description: The description of the disk(s) on the managed database.
                    type: number
                  planReplicas:
                    description: The number of standby nodes available on the managed
                      database (excluded for Kafka engine types).
                    type: number
                  publicHost:
                    description: The public hostname assigned to the managed database
                      (VPC-attached only).
                    type: string
                  readReplicas:
                    description: A list of read replicas attached to the managed database.
                    items:
                      properties:
                        backupHour:
                          description: The preferred hour of the day (UTC) for daily
                            backups to take place (unavailable for Kafka engine types).
                          type: string
                        backupMinute:
                          description: The preferred minute of the backup hour for
                            daily backups to take place (unavailable for Kafka engine
                            types).
                          type: string
                        evictionPolicy:
                          description: The configuration value for the data eviction
                            policy on the managed database (Valkey engine types only
                            - noeviction, allkeys-lru, volatile-lru, allkeys-random,
                            volatile-random, volatile-ttl, volatile-lfu, allkeys-lfu).
                          type: string
                        ferretdbCredentials:
                          additionalProperties:
                            type: string
                          type: object
                          x-kubernetes-map-type: granular
                        label:
                          description: A label for the managed database.
                          type: string
                        mysqlLongQueryTime:
                          description: The configuration value for the long query
                            time (in seconds) on the managed database (MySQL engine
                            types only).
                          type: number
                        mysqlRequirePrimaryKey:
                          description: The configuration value for whether primary
                            keys are required on the managed database (MySQL engine
                            types only).
                          type: boolean
                        mysqlSlowQueryLog:
                          description: The configuration value for slow query logging
                            on the managed database (MySQL engine types only).
                          type: boolean
                        mysqlSqlModes:
                          description: A list of SQL modes to configure for the managed
                            database (MySQL engine types only - ALLOW_INVALID_DATES,
                            ANSI, ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                            IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE,
                            NO_ENGINE_SUBSTITUTION, NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE,
                            NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY, PIPES_AS_CONCAT,
                            REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                            TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        planDisk:
                          description: The description of the disk(s) on the managed
                            database.
                          type: number
                        publicHost:
                          description: The public hostname assigned to the managed
                            database (VPC-attached only).
                          type: string
                        region:
                          description: The ID of the region that the managed database
                            is to be created in. See List Regions
                          type: string
                        tag:
                          description: The tag to assign to the managed database.
                          type: string
                        trustedIps:
                          description: A list of allowed IP addresses for the managed
                            database.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  region:
                    description: The ID of the region that the managed database is
                      to be created in. See List Regions
                    type: string
                  saslPort:
                    description: The SASL connection port for the managed database
                      (Kafka engine types only).
                    type: string
                  schemaRegistryUri:
                    description: The URI to access the Schema Registry service of
                      your Kafka cluster if Schema Registry is enabled (Kafka engine
                      types only).
                    type: string
                  tag:
                    description: The tag to assign to the managed database.
                    type: string
                  trustedIps:
                    description: A list of allowed IP addresses for the managed database.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  vpcId:
                    description: The ID of the VPC Network to attach to the Managed
                      Database.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessCert:
                    description: The certificate to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  accessKey:
                    description: The private key to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  backupHour:
                    description: The preferred hour of the day (UTC) for daily backups
                      to take place (unavailable for Kafka engine types).
                    type: string
                  backupMinute:
                    description: The preferred minute of the backup hour for daily
                      backups to take place (unavailable for Kafka engine types).
                    type: string
                  clusterTimeZone:
                    description: The configured time zone for the Managed Database
                      in TZ database format (e.g. UTC, America/New_York, Europe/London).
                    type: string
                  databaseEngine:
                    description: The database engine of the new managed database.
                    type: string
                  databaseEngineVersion:
                    description: The database engine version of the new managed database.
                    type: string
                  enableKafkaConnect:
                    description: The configuration value for Kafka Connect support
                      (Kafka engine types only).
                    type: boolean
                  enableKafkaRest:
                    description: The configuration value for Kafka REST support (Kafka
                      engine types only).
                    type: boolean
                  enableSchemaRegistry:
                    description: The configuration value for Schema Registry support
                      (Kafka engine types only).
                    type: boolean
                  evictionPolicy:
                    description: The configuration value for the data eviction policy
                      on the managed database (Valkey engine types only - noeviction,
                      allkeys-lru, volatile-lru, allkeys-random, volatile-random,
                      volatile-ttl, volatile-lfu, allkeys-lfu).
                    type: string
                  ferretdbCredentials:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  kafkaRestUri:
                    description: The URI to access the RESTful interface of your Kafka
                      cluster if Kafka REST is enabled (Kafka engine types only).
                    type: string
                  label:
                    description: A label for the managed database.
                    type: string
                  maintenanceDow:
                    description: The preferred maintenance day of week for the managed
                      database.
                    type: string
                  maintenanceTime:
                    description: The preferred maintenance time for the managed database.
                    type: string
                  mysqlLongQueryTime:
                    description: The configuration value for the long query time (in
                      seconds) on the managed database (MySQL engine types only).
                    type: number
                  mysqlRequirePrimaryKey:
                    description: The configuration value for whether primary keys
                      are required on the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSlowQueryLog:
                    description: The configuration value for slow query logging on
                      the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSqlModes:
                    description: A list of SQL modes to configure for the managed
                      database (MySQL engine types only - ALLOW_INVALID_DATES, ANSI,
                      ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                      IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE, NO_ENGINE_SUBSTITUTION,
                      NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE, NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY,
                      PIPES_AS_CONCAT, REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                      TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  password:
                    description: The password for the managed database's primary admin
                      user.
                    type: string
                  plan:
                    description: The ID of the plan that you want the managed database
                      to subscribe to. See List Managed Database Plans
                    type: string
                  planBrokers:
                    description: The number of brokers available on the managed database
                      (Kafka engine types only).
                    type: number
                  planDisk:
                    description: The description of the disk(s) on the managed database.
                    type: number
                  planReplicas:
                    description: The number of standby nodes available on the managed
                      database (excluded for Kafka engine types).
                    type: number
                  publicHost:
                    description: The public hostname assigned to the managed database
                      (VPC-attached only).
                    type: string
                  readReplicas:
                    description: A list of read replicas attached to the managed database.
                    items:
                      properties:
                        backupHour:
                          description: The preferred hour of the day (UTC) for daily
                            backups to take place (unavailable for Kafka engine types).
                          type: string
                        backupMinute:
                          description: The preferred minute of the backup hour for
                            daily backups to take place (unavailable for Kafka engine
                            types).
                          type: string
                        evictionPolicy:
                          description: The configuration value for the data eviction
                            policy on the managed database (Valkey engine types only
                            - noeviction, allkeys-lru, volatile-lru, allkeys-random,
                            volatile-random, volatile-ttl, volatile-lfu, allkeys-lfu).
                          type: string
                        ferretdbCredentials:
                          additionalProperties:
                            type: string
                          type: object
                          x-kubernetes-map-type: granular
                        label:
                          description: A label for the managed database.
                          type: string
                        mysqlLongQueryTime:
                          description: The configuration value for the long query
                            time (in seconds) on the managed database (MySQL engine
                            types only).
                          type: number
                        mysqlRequirePrimaryKey:
                          description: The configuration value for whether primary
                            keys are required on the managed database (MySQL engine
                            types only).
                          type: boolean
                        mysqlSlowQueryLog:
                          description: The configuration value for slow query logging
                            on the managed database (MySQL engine types only).
                          type: boolean
                        mysqlSqlModes:
                          description: A list of SQL modes to configure for the managed
                            database (MySQL engine types only - ALLOW_INVALID_DATES,
                            ANSI, ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                            IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE,
                            NO_ENGINE_SUBSTITUTION, NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE,
                            NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY, PIPES_AS_CONCAT,
                            REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                            TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        planDisk:
                          description: The description of the disk(s) on the managed
                            database.
                          type: number
                        publicHost:
                          description: The public hostname assigned to the managed
                            database (VPC-attached only).
                          type: string
                        region:
                          description: The ID of the region that the managed database
                            is to be created in. See List Regions
                          type: string
                        tag:
                          description: The tag to assign to the managed database.
                          type: string
                        trustedIps:
                          description: A list of allowed IP addresses for the managed
                            database.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  region:
                    description: The ID of the region that the managed database is
                      to be created in. See List Regions
                    type: string
                  saslPort:
                    description: The SASL connection port for the managed database
                      (Kafka engine types only).
                    type: string
                  schemaRegistryUri:
                    description: The URI to access the Schema Registry service of
                      your Kafka cluster if Schema Registry is enabled (Kafka engine
                      types only).
                    type: string
                  tag:
                    description: The tag to assign to the managed database.
                    type: string
                  trustedIps:
                    description: A list of allowed IP addresses for the managed database.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  vpcId:
                    description: The ID of the VPC Network to attach to the Managed
                      Database.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.databaseEngine is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.databaseEngine)
                || (has(self.initProvider) && has(self.initProvider.databaseEngine))'
            - message: spec.forProvider.databaseEngineVersion is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.databaseEngineVersion)
                || (has(self.initProvider) && has(self.initProvider.databaseEngineVersion))'
            - message: spec.forProvider.label is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.label)
                || (has(self.initProvider) && has(self.initProvider.label))'
            - message: spec.forProvider.plan is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.plan)
                || (has(self.initProvider) && has(self.initProvider.plan))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: DatabaseStatus defines the observed state of Database.
            properties:
              atProvider:
                properties:
                  accessCert:
                    description: The certificate to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  accessKey:
                    description: The private key to authenticate the default user
                      (Kafka engine types only).
                    type: string
                  backupHour:
                    description: The preferred hour of the day (UTC) for daily backups
                      to take place (unavailable for Kafka engine types).
                    type: string
                  backupMinute:
                    description: The preferred minute of the backup hour for daily
                      backups to take place (unavailable for Kafka engine types).
                    type: string
                  clusterTimeZone:
                    description: The configured time zone for the Managed Database
                      in TZ database format (e.g. UTC, America/New_York, Europe/London).
                    type: string
                  databaseEngine:
                    description: The database engine of the new managed database.
                    type: string
                  databaseEngineVersion:
                    description: The database engine version of the new managed database.
                    type: string
                  dateCreated:
                    description: The date the managed database was added to your Vultr
                      account.
                    type: string
                  dbname:
                    description: The managed database's default logical database.
                    type: string
                  enableKafkaConnect:
                    description: The configuration value for Kafka Connect support
                      (Kafka engine types only).
                    type: boolean
                  enableKafkaRest:
                    description: The configuration value for Kafka REST support (Kafka
                      engine types only).
                    type: boolean
                  enableSchemaRegistry:
                    description: The configuration value for Schema Registry support
                      (Kafka engine types only).
                    type: boolean
                  evictionPolicy:
                    description: The configuration value for the data eviction policy
                      on the managed database (Valkey engine types only - noeviction,
                      allkeys-lru, volatile-lru, allkeys-random, volatile-random,
                      volatile-ttl, volatile-lfu, allkeys-lfu).
                    type: string
                  ferretdbCredentials:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  host:
                    description: The hostname assigned to the managed database.
                    type: string
                  id:
                    description: The ID of the managed database.
                    type: string
                  kafkaRestUri:
                    description: The URI to access the RESTful interface of your Kafka
                      cluster if Kafka REST is enabled (Kafka engine types only).
                    type: string
                  label:
                    description: A label for the managed database.
                    type: string
                  latestBackup:
                    description: The date of the latest backup available on the managed
                      database.
                    type: string
                  maintenanceDow:
                    description: The preferred maintenance day of week for the managed
                      database.
                    type: string
                  maintenanceTime:
                    description: The preferred maintenance time for the managed database.
                    type: string
                  mysqlLongQueryTime:
                    description: The configuration value for the long query time (in
                      seconds) on the managed database (MySQL engine types only).
                    type: number
                  mysqlRequirePrimaryKey:
                    description: The configuration value for whether primary keys
                      are required on the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSlowQueryLog:
                    description: The configuration value for slow query logging on
                      the managed database (MySQL engine types only).
                    type: boolean
                  mysqlSqlModes:
                    description: A list of SQL modes to configure for the managed
                      database (MySQL engine types only - ALLOW_INVALID_DATES, ANSI,
                      ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                      IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE, NO_ENGINE_SUBSTITUTION,
                      NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE, NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY,
                      PIPES_AS_CONCAT, REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                      TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  password:
                    description: The password for the managed database's primary admin
                      user.
                    type: string
                  plan:
                    description: The ID of the plan that you want the managed database
                      to subscribe to. See List Managed Database Plans
                    type: string
                  planBrokers:
                    description: The number of brokers available on the managed database
                      (Kafka engine types only).
                    type: number
                  planDisk:
                    description: The description of the disk(s) on the managed database.
                    type: number
                  planRam:
                    description: The amount of memory available on the managed database
                      in MB.
                    type: number
                  planReplicas:
                    description: The number of standby nodes available on the managed
                      database (excluded for Kafka engine types).
                    type: number
                  planVcpus:
                    description: The number of virtual CPUs available on the managed
                      database.
                    type: number
                  port:
                    description: The connection port for the managed database.
                    type: string
                  publicHost:
                    description: The public hostname assigned to the managed database
                      (VPC-attached only).
                    type: string
                  readReplicas:
                    description: A list of read replicas attached to the managed database.
                    items:
                      properties:
                        backupHour:
                          description: The preferred hour of the day (UTC) for daily
                            backups to take place (unavailable for Kafka engine types).
                          type: string
                        backupMinute:
                          description: The preferred minute of the backup hour for
                            daily backups to take place (unavailable for Kafka engine
                            types).
                          type: string
                        clusterTimeZone:
                          description: The configured time zone for the Managed Database
                            in TZ database format (e.g. UTC, America/New_York, Europe/London).
                          type: string
                        databaseEngine:
                          description: The database engine of the new managed database.
                          type: string
                        databaseEngineVersion:
                          description: The database engine version of the new managed
                            database.
                          type: string
                        dateCreated:
                          description: The date the managed database was added to
                            your Vultr account.
                          type: string
                        dbname:
                          description: The managed database's default logical database.
                          type: string
                        evictionPolicy:
                          description: The configuration value for the data eviction
                            policy on the managed database (Valkey engine types only
                            - noeviction, allkeys-lru, volatile-lru, allkeys-random,
                            volatile-random, volatile-ttl, volatile-lfu, allkeys-lfu).
                          type: string
                        ferretdbCredentials:
                          additionalProperties:
                            type: string
                          type: object
                          x-kubernetes-map-type: granular
                        host:
                          description: The hostname assigned to the managed database.
                          type: string
                        id:
                          description: The ID of the managed database.
                          type: string
                        label:
                          description: A label for the managed database.
                          type: string
                        latestBackup:
                          description: The date of the latest backup available on
                            the managed database.
                          type: string
                        maintenanceDow:
                          description: The preferred maintenance day of week for the
                            managed database.
                          type: string
                        maintenanceTime:
                          description: The preferred maintenance time for the managed
                            database.
                          type: string
                        mysqlLongQueryTime:
                          description: The configuration value for the long query
                            time (in seconds) on the managed database (MySQL engine
                            types only).
                          type: number
                        mysqlRequirePrimaryKey:
                          description: The configuration value for whether primary
                            keys are required on the managed database (MySQL engine
                            types only).
                          type: boolean
                        mysqlSlowQueryLog:
                          description: The configuration value for slow query logging
                            on the managed database (MySQL engine types only).
                          type: boolean
                        mysqlSqlModes:
                          description: A list of SQL modes to configure for the managed
                            database (MySQL engine types only - ALLOW_INVALID_DATES,
                            ANSI, ANSI_QUOTES, ERROR_FOR_DIVISION_BY_ZERO, HIGH_NOT_PRECEDENCE,
                            IGNORE_SPACE, NO_AUTO_VALUE_ON_ZERO, NO_DIR_IN_CREATE,
                            NO_ENGINE_SUBSTITUTION, NO_UNSIGNED_SUBTRACTION, NO_ZERO_DATE,
                            NO_ZERO_IN_DATE, ONLY_FULL_GROUP_BY, PIPES_AS_CONCAT,
                            REAL_AS_FLOAT, STRICT_ALL_TABLES, STRICT_TRANS_TABLES,
                            TIME_TRUNCATE_FRACTIONAL, TRADITIONAL).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        password:
                          description: The password for the managed database's primary
                            admin user.
                          type: string
                        plan:
                          description: The ID of the plan that you want the managed
                            database to subscribe to. See List Managed Database Plans
                          type: string
                        planDisk:
                          description: The description of the disk(s) on the managed
                            database.
                          type: number
                        planRam:
                          description: The amount of memory available on the managed
                            database in MB.
                          type: number
                        planReplicas:
                          description: The number of standby nodes available on the
                            managed database (excluded for Kafka engine types).
                          type: number
                        planVcpus:
                          description: The number of virtual CPUs available on the
                            managed database.
                          type: number
                        port:
                          description: The connection port for the managed database.
                          type: string
                        publicHost:
                          description: The public hostname assigned to the managed
                            database (VPC-attached only).
                          type: string
                        region:
                          description: The ID of the region that the managed database
                            is to be created in. See List Regions
                          type: string
                        status:
                          description: The current status of the managed database
                            (poweroff, rebuilding, rebalancing, configuring, running).
                          type: string
                        tag:
                          description: The tag to assign to the managed database.
                          type: string
                        trustedIps:
                          description: A list of allowed IP addresses for the managed
                            database.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        user:
                          description: The primary admin user for the managed database.
                          type: string
                        vpcId:
                          description: The ID of the VPC Network to attach to the
                            Managed Database.
                          type: string
                      type: object
                    type: array
                  region:
                    description: The ID of the region that the managed database is
                      to be created in. See List Regions
                    type: string
                  saslPort:
                    description: The SASL connection port for the managed database
                      (Kafka engine types only).
                    type: string
                  schemaRegistryUri:
                    description: The URI to access the Schema Registry service of
                      your Kafka cluster if Schema Registry is enabled (Kafka engine
                      types only).
                    type: string
                  status:
                    description: The current status of the managed database (poweroff,
                      rebuilding, rebalancing, configuring, running).
                    type: string
                  tag:
                    description: The tag to assign to the managed database.
                    type: string
                  trustedIps:
                    description: A list of allowed IP addresses for the managed database.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  user:
                    description: The primary admin user for the managed database.
                    type: string
                  vpcId:
                    description: The ID of the VPC Network to attach to the Managed
                      Database.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
